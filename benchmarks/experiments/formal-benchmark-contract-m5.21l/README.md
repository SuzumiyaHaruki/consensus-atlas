# M5.21l FormalBenchmarkContract foundation

`summary.json` 由公开 synthetic contract fixture 机械重算，只验证三组 matching pair、六个 opaque
trial、三个 root-cause label、私有到公开投影和 target-neutral composition identity 解析。

fixture 和测试代码均已公开，因此不是 private holdout。当前尚未接入 fresh multi-pair evaluator，也尚未
实现 exposure audit，`formal_ready=false`。本阶段模型调用与新 SUT execution 均为零。
