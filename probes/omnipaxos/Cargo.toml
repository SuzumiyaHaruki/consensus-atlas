[package]
name = "consensus-atlas-omnipaxos-probe"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
omnipaxos = { version = "=0.2.2", features = ["serde"] }
omnipaxos_storage = "=0.2.2"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.10"
